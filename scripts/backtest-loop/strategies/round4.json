{
  "_comment": "轮 4 = 轮3 买门 + 对倒过滤 + 卖侧水下时间止损。买加 counterpartyOverlapRate<0.35（K组对倒重叠，轮1/轮3 两池预筛均 +0.06+）；卖加腿2 holdDuration>300s 且水下→退出（轮3 动量池反事实 Δ+0.018：stop15 16→6、强平 33→12；轮1 死票池上中性）",
  "tradeAmount": 0.1,
  "initialBalance": 100,
  "buyStrategies": [
    { "priority": 1, "condition": "tradeCount >= 3 AND firstBlockBuyShare < 0.85 AND bigHolderPresent == 0 AND maxBlockDropPct < 3 AND earlyReturn > 1 AND counterpartyOverlapRate < 0.35", "maxExecutions": 1 }
  ],
  "sellStrategies": [
    { "priority": 1, "condition": "profitPercent > 15 OR profitPercent < -15", "maxExecutions": 1 },
    { "priority": 2, "condition": "holdDuration > 300 AND profitPercent < 0", "maxExecutions": 1 }
  ]
}
