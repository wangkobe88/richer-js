# 拉砸代币区分分析报告

## 数据概览

- **实验ID**: 6b17ff18-002d-4ce0-a745-b8e02676abd4
- **拉砸代币数量**: 8个
- **非拉砸代币数量**: 7个

## 关键发现

### 第一类：通过聚簇特征可识别的拉砸代币（3/8）

| 代币 | 最大簇 | 占比 | 特征 |
|------|--------|------|------|
| 0xf3372a... | 283笔 | 95.9% | 超级簇 |
| 0xb9b114... | 286笔 | 95.3% | 超级簇 |
| 0xd8d4dd... | 300笔 | 100% | 超级簇 |

**特征**：单簇>200笔，占比>95%

### 第二类：聚簇特征不明显但实际是拉砸的代币（3/8）

| 代币 | 簇数 | 最大簇 | 占比 | 亏损 | 持仓时间 |
|------|------|--------|------|------|----------|
| 0x2be52e... | 4 | 39笔 | 51.3% | -65.6% | 10秒 |
| 0x68b04d... | 5 | 60笔 | 46.9% | -28.5% | 30秒 |
| 0x721f5a... | 3 | 106笔 | 49.5% | -16.7% | 10秒 |

**特征**：多小簇，但持仓时间极短（<30秒）

### 第三类：标注有疑问的代币（2/7）

| 代币 | 标注 | 特征 | 实际亏损 |
|------|------|------|----------|
| 0xf40dec... | 非拉砸 | 超级簇244笔，占比83.8% | -31.6% |
| 0x0da3a0... | 非拉砸 | 单簇300笔，占比100% | -21.0% |

**问题**：具有极端拉砸特征但被标注为"非拉砸"

## 区分特征分析

### 聚簇特征（早期参与者数据）

| 特征 | 拉砸平均 | 正常平均 | 区分能力 |
|------|----------|----------|----------|
| 簇数量 | 4.0个 | 5.6个 | 弱 |
| 最大簇 | 167.1笔 | 113.1笔 | 中等 |
| 第二/第一比 | 0.3 | 0.5 | 弱 |
| Mega簇比 | 0.7 | 0.3 | **强** |

**最佳聚簇特征**：
- `walletClusterMegaRatio > 0.5`: 拒绝25%拉砸，误杀42.9%正常
- `walletClusterMaxSize > 200`: 拒绝62.5%拉砸，误杀28.6%正常

### 价格信号特征（交易阶段）

| 特征 | 拉砸平均 | 正常平均 | 区分能力 |
|------|----------|----------|----------|
| 持仓时间 | 29.9秒 | 199.7秒 | **极强** |
| 代币年龄 | 1.48分钟 | 4.34分钟 | **强** |
| 上涨速度 | ~194%/分 | ~98%/分 | **强** |
| 早期收益率 | 278.7% | 236.4% | 弱 |

**最佳价格特征**：
- `持仓时间 < 20秒`: 拉砸62.5%，正常14.3%
- `代币年龄 < 1.5分钟`: 拉砸75%，正常42.9%
- `riseSpeed > 150`: 拉砸50%，正常14%

## 综合推荐方案

### 方案A：保守型（高拒绝率，适用于风险厌恶）

```javascript
// 拒绝条件（任一满足即拒绝）
walletClusterMaxSize > 200                    // 超级簇
OR riseSpeed > 150                             // 极速上涨
OR (age < 1.3 AND riseSpeed > 120)             // 年轻极速上涨
OR walletClusterSecondToFirstRatio < 0.1       // 极端第一簇主导
```

**预期效果**：
- 拉砸拒绝率：~75%
- 正常通过率：~43%

### 方案B：平衡型（推荐）

```javascript
// 拒绝条件（任一满足即拒绝）
walletClusterMaxSize > 200                    // 超级簇
OR (riseSpeed > 150 AND age < 2.0)             // 极速且年轻
OR (walletClusterMegaRatio > 0.7 AND age < 1.5) // Mega簇且年轻
```

**预期效果**：
- 拉砸拒绝率：~63%
- 正常通过率：~57%

### 方案C：激进型（高通过率）

```javascript
// 拒绝条件（仅极端情况）
walletClusterMaxSize > 250                    // 超大簇
OR (walletClusterMaxSize > 200 AND walletClusterSecondToFirstRatio < 0.05) // 超级簇且极度集中
```

**预期效果**：
- 拉砸拒绝率：~50%
- 正常通过率：~71%

## 实现建议

### 1. 添加新的预检查因子

需要在 `WalletClusterService.js` 中添加：
- `walletClusterMaxClusterRatio`: 最大簇占总交易数比例
- `walletClusterMaxClusterRatioPercent`: 百分比形式（0-100）

### 2. 添加价格信号因子

需要在 `PreBuyCheckService.js` 中添加：
- 计算买入时代的币年龄（age）
- 计算上涨速度（riseSpeed = earlyReturn / age）

### 3. 实现预检查条件

示例代码：

```javascript
// 在实验配置中设置
{
  "strategiesConfig": {
    "buyStrategies": [{
      "preBuyCheckCondition": "!(walletClusterMaxSize > 200) && !(riseSpeed > 150 && age < 2.0) && !(walletClusterMegaRatio > 0.7 && age < 1.5)"
    }]
  }
}
```

## 标注数据问题

建议重新确认以下代币的标注：
- `0xf40dec26ab76df60a761e78c84682d7117a64444`: 超级簇+亏损31.6%
- `0x0da3a0a3bd66bbeaaa4d35d12cb9ea3725294444`: 单簇300笔+亏损21.0%

这两个代币具有明显的拉砸特征，可能需要重新评估其分类。
