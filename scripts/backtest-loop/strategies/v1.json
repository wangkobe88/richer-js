{
  "_comment": "V1 = E1（c0150101）的虚拟实验孪生：买侧活跃度单门（buyVolumeBnb>=1.5）+ 叙事放行含 unrated（==9）+ 叙事直调挂同一活跃度门；卖侧分档 trailing 同 E1。与 e1.json 唯一差异是 trading_mode=virtual（实时 WSS）。叙事直调跑 J1.10（name_referent 阻断：截词/指向无名对象/知名非超级IP 均拦截）。跑前需失效 token_narrative 旧缓存（is_valid=false），避免命中 E1 期间写入的 J1.8 评级（CONVICTION/YAYA 等误 high）",
  "tradeAmount": 0.1,
  "initialBalance": 100,
  "buyStrategies": [
    {
      "priority": 1,
      "condition": "buyVolumeBnb >= 1.5 AND age < 30",
      "maxExecutions": 1,
      "preBuyCheckCondition": "narrativeRating == 2 OR narrativeRating == 3 OR narrativeRating == 9",
      "narrativeCallCondition": "buyVolumeBnb >= 1.5 AND age < 30"
    }
  ],
  "sellStrategies": [
    { "priority": 1, "condition": "drawdownFromHighestSinceLastBuy <= -45 AND holdDuration < 60", "maxExecutions": 1 },
    { "priority": 2, "condition": "drawdownFromHighestSinceLastBuy <= -35 AND holdDuration >= 60 AND holdDuration < 180", "maxExecutions": 1 },
    { "priority": 3, "condition": "drawdownFromHighestSinceLastBuy <= -25 AND holdDuration >= 180 AND holdDuration < 300", "maxExecutions": 1 },
    { "priority": 4, "condition": "drawdownFromHighestSinceLastBuy <= -20 AND holdDuration >= 300 AND holdDuration < 600", "maxExecutions": 1 },
    { "priority": 5, "condition": "drawdownFromHighestSinceLastBuy <= -10 AND holdDuration >= 600", "maxExecutions": 1 }
  ]
}
